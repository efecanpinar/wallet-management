@*<link href="~/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
<link href="~/assets/css/userlogin.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">*@
<link href="~/css/login.css" rel="stylesheet" />
@model PortfolioManagement.Application.DTOs.SignInUserDto
@*<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-5">
            <div class="card shadow-lg border-0 rounded-lg mt-5">
                <div class="card-header"><h3 class="text-center font-weight-light my-4"><span class="fas fa-house-user"></span> Giriş Yap</h3></div>
                <div class="card-body">
                    @using (Html.BeginForm("SignIn", "Login", FormMethod.Post))
                    {
                        <div class="form-sample">
                            <div class="form-group">
                                @Html.LabelFor(x => x.UserName )
                                @Html.TextBoxFor(x => x.UserName, new { @class = "form-control" })
                            </div>
                            <div class="form-group">
                                @Html.LabelFor(x => x.UserPassword)
                                @Html.TextBoxFor(x => x.UserPassword, new { @class = "form-control" })
                            </div>
                        </div>

                        <button class="btn btn-warning">Giriş Yap</button>

                    }
                </div>
                <div class="card-footer text-center">
                    <button class="btn btn-primary" type="button" onclick="location.href='@Url.Action("Register", "Login")'"><span class="fas fa-sign-in-alt"></span> Kayıt Ol</button>
                </div>
            </div>
        </div>
    </div>
</div>*@

<div class="login">
    <h1>Login</h1>
    @using (Html.BeginForm("SignIn", "Login", FormMethod.Post))
    {
        @Html.TextBoxFor(x => x.UserName, new{@class = "input", placeholder="Kullanıcı adı"})
        @Html.TextBoxFor(x => x.UserPassword, new{@class = "input", placeholder="Şifre"})
        <button type="submit" class="btn btn-primary btn-block btn-large">Giriş yap</button>
    }
</div>